# .NET build output
backend/**/bin/
backend/**/obj/

# Local SQLite database files
backend/**/*.db
backend/**/*.db-shm
backend/**/*.db-wal

# Node
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/

# Local secrets — real DB credentials for this machine live here, never in
# appsettings.json (which stays a generic placeholder and is committed).
backend/**/appsettings.Development.json
